VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only (Vercel serverless function) — never prefix with VITE_, never expose to client bundle
SUPABASE_SERVICE_ROLE_KEY=

# Google Cloud Console > Maps JavaScript API, restrict by HTTP referrer
VITE_GOOGLE_MAPS_API_KEY=

# Server-only (Vercel serverless function) — used by the text-import parser
OPENAI_API_KEY=
